const fs = require('fs');
// const ffmpegPath = require('ffmpeg-downloader').path

const ffmpegPathSet = (() => {
  // const _path = require('ffmpeg-downloader').path;


  // process.env.FFMPEG_PATH = _path;

  console.log('\nprocess.env.FLUENTFFMPEG_COV: '+process.env.FLUENTFFMPEG_COV)
  // console.log('\nprocess.env.FFMPEG_PATH: '+process.env.FFMPEG_PATH)
})();
